name: dcenv # The name of your enviroment
channels:
  - conda-forge
  - anaconda
  - defaults
dependencies:
  - python=3.8  
  - notebook
  - nb_conda_kernels
  - scipy
  - matplotlib
  - numpy
  - pandas
  - plotly
  - dash
  - dash-bootstrap-components
  - pip:
    - jupyter-book  # Installed via pip
